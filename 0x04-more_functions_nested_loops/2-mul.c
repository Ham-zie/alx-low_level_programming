#include <stdio.h>
/**
 * Description: mul - a function that multipies two integers
 * @a: first input 
 * @b: second input 
 * Return: multiplication result of the 2 inputs 
 */
int mul(int a, int b)
{	
	printf("%d\n", mul(98, 1024));
	printf("%d\n", mul(-402, 4096));	 
	return (a * b);
}
